// Generated by Unloop - 2026-01-05T17:25:34.203Z
// Office Thermostat: Smart temperature control for home office environment

import { MaterialWindow } from "@material";
import { AppData } from "data.slint";
import { HomeScreen } from "screens.slint";
import { ControlScreen } from "screens.slint";
import { SettingsScreen } from "screens.slint";

export component App inherits MaterialWindow {
    title: "Office Thermostat";
    width: 800px;
    height: 480px;
    background: #1E1E1E;
    
    in-out property <string> active_screen: "home";
    in-out property <AppData> data: {
        current_temp: 72,
        target_temp: 72,
        humidity: 45,
        hvac_status: "ok",
        wifi_status: "ok",
        temp_unit: "°F",
        brightness: 80,
        auto_dim: true,
        temp_offset: 0,
        schedule_enabled: false,
        device_version: "1.0.0",

    };
    
    callback quick-cool-clicked();
    callback schedule-clicked();
    callback target-changed(float);
    callback mode-changed(string);
    
    VerticalLayout {
        Rectangle {
            vertical_stretch: 1;
            
            if active-screen == "home" : HomeScreen {
                data <=> root.data;
            }

            if active-screen == "control" : ControlScreen {
                data <=> root.data;
            }

            if active-screen == "settings" : SettingsScreen {
                data <=> root.data;
            }

        }
        
        Rectangle {
            height: 80px;
            background: #2A2A2A;
            
            HorizontalLayout {
                alignment: space-evenly;
                
                Rectangle {
                    horizontal_stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "home"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Home";
                            font_size: 12px;
                            color: active-screen == "home" ? #3AA1F5 : #9E9E9E;
                            horizontal_alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal_stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "control"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Control";
                            font_size: 12px;
                            color: active-screen == "control" ? #3AA1F5 : #9E9E9E;
                            horizontal_alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal_stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "settings"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Settings";
                            font_size: 12px;
                            color: active-screen == "settings" ? #3AA1F5 : #9E9E9E;
                            horizontal_alignment: center;
                        }
                    }
                }
            }
        }
    }
}
